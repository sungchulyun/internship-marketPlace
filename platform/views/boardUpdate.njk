<!DOCTYPE html>
<head>
<meta charset="UTF-8">
<title>판매 게시판</title>
</head>
<body>
    <div id="board_write">
        <h1>판매 글 수정 페이지입니다.</h1>
            <div id="write_area">
                <form action="/boards/updatePro/{{board.id}}" method="post" enctype="multipart/form-data">
                            
                    <div id="in_title">
                        <textarea name="id" id="id" rows="1" cols="55" placeholder="{{board.id}}" maxlength="100" required></textarea>
                    </div>
                   
                    <div id="in_title">
                        <textarea name="title" id="title" rows="1" cols="55" placeholder="{{board.title}}" maxlength="100" required></textarea>
                    </div>
 
                    <div class="wi_line"></div>
                    <div id="in_content">
                        <textarea name="content" id="content" placeholder="{{board.content}}" required></textarea>
                    </div>

                     <div id="in_content">
                        <textarea name="prize" id="prize" placeholder="{{board.prize}}" required></textarea>
                    </div>
 
                     <div id="in_content">
                         <input type="file" name="image" placeholder="{{board.image}}" />
                    </div>
                     
 
 
                    <div class="bt_se">
                        <button type="submit">수정하기</button>
                    </div>
                </form>
            </div>
        </div>
    </body>
</html>